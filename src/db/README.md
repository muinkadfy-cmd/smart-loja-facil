# SQLite

O schema real é aplicado em `src-tauri/src/main.rs` para garantir que o banco principal fique no lado Tauri/Rust e não no navegador.

Esta pasta fica reservada para futuras migrations versionadas.
